# Known limitations

The local demo is synthetic and does not prove a deployed Gemini request.
The legacy analysis adapter remains for compatibility while the role provider
path is rolled out. Native Android parity is outside the hackathon web slice
and is documented by ADR. A cloud proof requires a configured GCP project.
