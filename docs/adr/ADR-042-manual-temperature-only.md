# ADR-042 — Temperature entry is manual only

PETi Phase 6 accepts a value from a user-entered thermometer reading or other
explicit source. The phone is never presented as a core-temperature sensor and
no AI temperature estimate is generated by this phase.
