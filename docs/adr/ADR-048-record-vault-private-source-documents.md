# ADR-048 — Record Vault private source documents

Veterinary records reuse Phase-3 `DOCUMENT_SOURCE` media with the
`CLINICAL_DOCUMENT` retention class. Record metadata is owner- and pet-scoped;
document bytes are never exposed through permanent public URLs.
