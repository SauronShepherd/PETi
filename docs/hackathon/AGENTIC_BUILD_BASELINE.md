# Agentic build baseline

This file records the repository baseline for the hackathon multi-agent build.
The candidate SHA must be regenerated by CI at release time; local working-tree
changes are not evidence of a released candidate.

Scope: client-neutral backend contracts plus the existing `web/` judge surface.
Native Android parity remains outside this hackathon slice and is documented
in the scope ADR.
