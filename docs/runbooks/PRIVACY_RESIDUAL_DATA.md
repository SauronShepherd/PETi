# Privacy residual-data runbook

Freeze the account, stop new work, run the deletion dependency plan, and verify
Firestore and object-store counts independently. Any residual is a release
blocker; reconcile and repeat verification before marking deletion complete.
