# Pet History Assistant Release Certificate Contract

The certificate binds assistant schema, citation validator, safety policy, prompt version, search authorization, funding policy, evaluation manifest and operational flags. This source artifact records the required binding; external model and production certification remain separate gates.
