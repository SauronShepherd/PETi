# Phase 2 credit contracts

The backend is the authority for these versioned concepts: `OperationType`, `CostProfile`, `CreditGrant`, `CreditReservation`, `CreditLedgerEntry`, `FundingQuote`, and reward intents. The web client consumes API DTOs and never writes economic state directly.
