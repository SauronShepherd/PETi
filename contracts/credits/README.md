# Phase 2 credit contracts

The backend is the authority for these versioned concepts: `OperationType`, `CostProfile`, `CreditGrant`, `CreditReservation`, `CreditLedgerEntry`, `FundingQuote`, and reward intents. Android must consume API DTOs and never write economic state directly.
