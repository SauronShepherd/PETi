# Agent feature contract

The Android agent surface is durable and resumable: it creates authenticated sessions/runs, renders queued, running, waiting, partial and completed states, displays evidence/provenance, and never treats model output or tool arguments as navigation commands.
